#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../strip-whitespace/.bin/strip-whitespace" "$@"
  ret=$?
else 
  node  "$basedir/../strip-whitespace/.bin/strip-whitespace" "$@"
  ret=$?
fi
exit $ret
